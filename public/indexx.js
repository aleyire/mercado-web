const agregarCarrito = document.querySelector("#agregar-carrito")
const modalCerrar = document.querySelector("#modal-cerrar") // es la equis
const btnCerrar = document.querySelector("#btn-cerrar") // boton de abajo
const nombre = document.querySelector("#nombre")//falta
const btnNavbar = document.querySelector("#btn-navbar")// enlazar con el carrito de compras
const comprar = []

/* agregar productos al carrito
const carrito = (e) => {
  let producto = 1
  producto++
}
agregarCarrito.forEach((item) => {
  item.addEventListener("click", carrito) 
})
// mostrar el carrito en el modal
btnNavbar.addEventListener("click", (e) => {
  e.preventDefault()
  nombre.innerHTML = comprar.join(", ")
})
// cerrar el modal
modalCerrar.addEventListener("click", (e) => {
  e.preventDefault()
  modalCerrar.classList.remove("show")
})
btnCerrar.addEventListener("click", (e) => {
  e.preventDefault()
  modalCerrar.classList.remove("show")
})*/


